<h2>Relatórios</h2>
<h3 class="text-center">{{obras.nomeObra}}</h3>
<div class="card mt-4">
  <div class="card-body">
    <small>Proprietário: {{obras.nomeProprietario}}</small>
    <br>
    <small>Data de início: {{obras.data}}</small>
    <br>
    {{#each semanas}}
    <small>Semana [{{data}}] Total R$: {{total}}</small>
    <br>
    {{else}}
    {{/each}}
    <small>Total gasto em Obra R$ {{totalgeral.totalGeral}}</small>
  </div>
</div>